doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Admin Dashboard
    link(rel='stylesheet', type='text/css', href='/css/styles.css')
  body
    #main
      .headerContainer
        .logo
          | IHS Logo
        .headerButtons
          a(href='/adminView.html') Home
          a(href='/adminView.html') Preferences
          a(href='/adminView.html') Help
        .logout
          button(type='button')
            | Log Out
        .notification
          | [  ]
      .title
        h1 Dashboard
      .filter
        input(placeholder='filter By')
      br
      .ticketTable
        each val, index in json
          if index === 0
            .headrow.row
              .headColumn.col.ticketId
                | Ticket ID
              .headColumn.col.createdAt
                | Received
              .headColumn.col.address
                | Location
              .headColumn.col.description
                | Short Description
              .headColumn.col.status
                | Status
              .headColumn.col.username
                | Submitter
              .headColumn.col.phoneNumber
                | Phone #
              .headColumn.col.email
                | Email
              .headColumn.col.pictureCol
                | Picture
          else
            .row
              .col.ticketId
                | #{val.id}
              .col.createdAt
                | #{val.formatDate}
              .col.address
                | #{val.address}
              .col.description
                | #{val.description}
              .col.status
                form(action='/homeless/' + val.id + "/?_method=put", method="post")
                  select(name="refferalStatus_id", onchange='this.form.submit()')
                    option(value="" disabled selected) #{val.refferalStatus_id}
                    option(value=1) 1
                    option(value=2) 2
                    option(value=3) 3
                    option(value=4) 4
              .col.username
                | #{val.name}
              .col.phoneNumber
                | #{val.phoneNumber}
              .col.email
                | #{val.email}
              .col.pictureCol
                a(href="/homeless/" + val.id + "/photo")
                  | Picture
      br
      br
      br
      br
      br
      .container
        h2 Homeless Map Tracker
        .options-box
          h1 Toggle
          div.toggleContainer
            input#show-listings(type='button', value='Show Listings')
            input#hide-listings(type='button', value='Hide Listings')
        .destinations-box
          input#origin-input.controls(type='text', placeholder='Enter start location')
          input#destination-input.controls(type='text', placeholder='Enter end location')
        select#mode-selector
          option(value='DRIVING') Driving
          option(value='WALKING') Walking
        #map
      script(type='text/javascript', src='/js/map.js')
      script(async='', defer='', src='https://maps.googleapis.com/maps/api/js?libraries=places&key=AIzaSyCVY-LYZbEIkWbG78IjjFmBTRalFKfrIFg&v-3&callback=initMap')